指摘ver1

- オンボーディングフローの強化
現状のシステムでは離脱率が高くなる恐れあり。

- DDG returned no results, using fail-safe mock for demo.への対応
何を意味しているログか解明 -> 対応

- 永続化層（DB/Redis）の追加、ユーザーアカウント機能
ユーザーアカウント機能がないことによる履歴の管理不可、またセッション管理もできない。

- sandbox生成のセキュリティ
ネットワークからの情報取得時に悪意のあるコードがある場合問題。また生成時のコードの安全性も考慮する必要がある。

- 返答言語固定化(返答) => 各言語設定


- concept-compliant-types.ts
    - fact.sameCriteriaUsed || fact.lastDelta.nodeDelta === 0
    ゼロだと少しでも変化があった場合(たとえば0.1みたいな)も同じ判定になるため、一度範囲について相談すべき。

    - 視点操作子の型定義が静的
    concept-compliant-types.tsのtranslation_templatesについて、視点操作子の型の詳細や対応をテンプレートとして定義しているため。llmは干渉できるが、templateによる定義なため静的になりやすい。これは少しだけ設計違反。
    本来であれば定義内容は概念や内容の説明であり、それをもとにllmが動的に生成すべき。

    - fact.sameOperatorRepeated >= 2
    2回が適切かどうかを一度相談すべき。2回繰り返していることが変化していないとは限らないかも。

    - translateQuestionToLanguageについて
    実装が正しいかどうか不安。'次に何が見えますか？'や'それ'を実装に入れていることがtemplate実装を引き起こす可能性。

- structure-generation-types.ts
    - QuestionStructureTypeについて
    問いの種類が設計に準拠しているのかが疑問。設計に準拠するともっと種類が増えるかも。

    - StructureNodeの定義について
    禁止プロパティと投影すべき情報が混同しているかも。
    サービス側が判断して勝手に情報を加算するのと、ユーザー側が理解して、それが入力から判明した段階で投影するのでは違う。ここは何とかしないとかもね。
    
    - StructureNodeが持つ要素について記述がないからわかりずらい。
    説明も求む

    - StructureNodeのreliabilityについて
    具体的なパーセントを設定するか、その判定ロジック部分が適切か確認する必要がある。

    - 2.ExternalResourcePool Typesは何のための実装？
    説明求む

    - 3.OperatorLogEntryについて
    何を実装しているがこちら側はあまり理解していないが、ツッコむ。
    判断が「できたか」ではなく「判断軸を使おうとしたかどうか」というのが観測対象で、仮にサービスがの動きに関するロジックならば判断軸のみの観測では足りないのでは？また実装を見る感じ判断軸以外の観測も行っている。

    - 4.Search Query Types (RAG用)の実装内容がわからない。
    説明求む

    - SystemPhase: システムフェーズについてロジックを詳しく知りたい。
    ユーザの状態にきちんと対応しているロジックなら、ここは正しい実装だよね。でも他と役割かぶってるんじゃない？わかんない。

    - 7.Lost State Typesの"保持をしない"という記述の意味が分からない。

    - 8.Intervention Typesについて、
    layerは機能としての区別で、時間軸的な状態をもつものだったか忘れたから、設計確認しつついろいろ修正しないとかもね。

    - 9.Utility Functions
    空のStructuralDelta(nodeだよねこれ)を作成したあと、いろいろあって空のノードがそのまま出力されてない？
    わかんないけど。

- 選択肢のソートについて
選択肢のソートをサービス側が提案(強制とかサービスが操るってことじゃないよ)することがあると設計書から読み取れると思うんだけど、現状だと会話から意図検出や投影として設定するだけで、UIで操作はできないよね、これって結果的にサービス側がUI操作から意図検出できないんじゃない？あとここでの意図検出ていうのは、実装されているやつのことだから。ひとつ気になるのは、実装されているユーザー側のUI操作による意図検出ロジックってどうやってるの？選択肢ソート以外に判断する方法なくない？わからんくなってきた。

- ユーザー側が投影されたドキュメントとか、ノードの名前とか、要素とかいろいろ(これらはnodeとか選択肢が持ってるやつだよ)編集できた方がいいかもね。
そうしないと意図が違うやつが生成されたり、チャットからの入力しかできなくなる。あとノードの追加、接続とかできるとよりいいかも。選択肢はユーザーが追加しなくてもいいだろうね。だってさすがにそれはチャットで入力したいだろうから。

- ユーザー側に公開されている選択肢やノードの持つ情報が少なくない？選択肢はUIで属性ごとに並び替え(自動で)できるようになってるけど、その要因が見えないとわかりずらいと思うよ。

- noteみたいな見た目のやつ作る機能って確かサンドボックスとは別での実装だよね、いまいちどのタイミングでどこにどんな感じで生成(表示)されるのかわからない。

- 現状UIで、ノードグラフはタブに表示されていないのに横のツリーみたらノードグラフが見れる状態だよね。
ここもう少しどうするか相談した方がいいかも。

- 操作しログとかいろいろあるけど、履歴をどの程度参照するかのロジックは、現状どのロジックであろうと一つ一つ見直すべきだろうね。

- なんか一部ファイルでロジックの実装コードが合理的じゃないやつがある気がする。どうだろう。
あとそれに近いけどファイル間の接続も似たような状況のとこあるかもね。

- UI_TEXTSのQUESTIONSのところどうなの？なんか定義してあるけど。
実装が複雑すぎてこれがなんのロジックかわからんのよね。

- 設計書の一部がミスってるから実装が複雑化したりなんなり色々してるのかも。わかんねー

指摘ver2

- ユーザーの突然の不規則な入力に対応できていない
例: 「こんにちは」という入力に「不明な概念と「こんにちは」は何の関係があるのか?」と返したりなど。
これはサービスの安定性に関わるよ。
